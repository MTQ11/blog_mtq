<form class="mt-4" method="POST" action="/obj/{{objs._id}}?_method=PUT">
      <div class="form-group">
            <label>Tên</label>
            <input type="text" name="name" class="form-control" value="{{objs.name}}">
            <label>Tuổi</label>     
            <input type="text" name="age" class="form-control" value="{{objs.age}}">
      </div>
      <button type="submit" class="btn btn-primary">Cập Nhật</button>
</form>